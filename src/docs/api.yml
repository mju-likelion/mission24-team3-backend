paths:
  /:
    get:
      summary: "Get 방식 (1)"
      description: "서버에 아무 데이터 업이 Get방식으로 요청"
      tags:
        - Get 방식
      responses:
        "200":
          description: 서버에게 받은 결과 값
          content:
            application/json:
              schema:
                type: object
                properties:
                  ok:
                    type: boolean
